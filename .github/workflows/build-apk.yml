name: Build APK
on:
  workflow_dispatch:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Install dependencies
        run: |
          npm install
          python3 -m pip install pillow -q

      - name: Ensure Android platform + sync
        run: |
          if [ ! -d android ]; then
            echo "android/ missing — adding platform"
            npx cap add android
          fi
          npx cap sync android
          ls -la android
          ls -la android/gradlew || true

      - name: Ensure gradlew exists
        run: |
          if [ ! -f android/gradlew ]; then
            echo "gradlew missing — recreating android platform"
            rm -rf android
            npx cap add android
            npx cap sync android
          fi
          chmod +x android/gradlew
          ls -la android/gradlew

      # Branding step removed for now (scripts/install-branding.py is missing)

      - name: Build debug APK
        run: cd android && ./gradlew assembleDebug

      - uses: actions/upload-artifact@v4
        with:
          name: orbitbills-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
